import fs from 'fs/promises';

const HistoryService = {
    saveCity: async (city) => {
        // Logic to save city to search history (e.g., append to a JSON file)
    },
    getHistory: async () => {
        // Logic to read and return the search history from the JSON file
    }
};

export default HistoryService;